- name: Deploy Symphony on Kubernetes
  hosts: appserver
  become: true

  tasks:
    - name: Ensure kubectl is installed
      apt:
        name: kubectl
        state: present
        update_cache: yes

    - name: Apply Kubernetes manifest
      command: kubectl apply -f /home/admin/symphony/k8s/symphony.yaml --validate=false
